<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.managementdna">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ManagementDNA">
        <activity android:name=".TaskCard"></activity>
        <activity android:name=".addNewTask" />
        <activity android:name=".Project.ProjectList" />
        <activity android:name=".projectTaskandCost" />
        <activity android:name=".Project.ProjectDetails" />
        <activity android:name=".Login" />
        <activity android:name=".Project.MainActivity" />
        <activity android:name=".Register">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>